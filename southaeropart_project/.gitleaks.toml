[extend]
useDefault = true

[allowlist]
description = "Only explicit nonfunctional CI fixture values; test files are scanned"
regexTarget = "secret"
regexes = [
    '''^ci_test_[A-Za-z0-9_]+$''',
    '''^(sk_test|pk_test|whsec|re)_ci_mock_[A-Za-z0-9_]+$''',
]
