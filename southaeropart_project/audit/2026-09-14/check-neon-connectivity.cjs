const fs=require('fs');const {createRequire}=require('module');const req=createRequire(process.cwd()+'/apps/storefront/package.json');req('dotenv').config({path:'.env',quiet:true});const {neon}=req('@neondatabase/serverless');
(async()=>{const sql=neon(process.env.DATABASE_URL);try{const info=await sql`SELECT current_database() IS NOT NULL AS connected`;console.log(JSON.stringify(info));const schemas=await sql`SELECT nspname FROM pg_namespace WHERE nspname LIKE 'security_test_%'`;console.log(JSON.stringify({testSchemas:schemas.map(r=>r.nspname)}));}catch(e){console.log(JSON.stringify({error:e.code||e.name,message:e.message?.replace(/postgres(?:ql)?:\/\/\S+/g,'[REDACTED]')}));process.exitCode=1}})();
