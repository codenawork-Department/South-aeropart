// Retired: the old runner mutated shared fixtures and lacked request context.
throw new Error("Use pnpm verify or pnpm verify:stripe with ALLOW_ISOLATED_SECURITY_TESTS=true; each run creates and removes its own schema.");
export {};
